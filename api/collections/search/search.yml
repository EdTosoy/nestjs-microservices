info:
  name: search
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/search?q=har
  params:
    - name: q
      value: har
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
